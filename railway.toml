[build]
builder = "nixpacks"
buildCommand = "pnpm install --frozen-lockfile"

[deploy]
startCommand = "npx trigger.dev@latest worker"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
